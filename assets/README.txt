Place your resume PDF in this folder as:

  resume.pdf

Both Download Resume buttons in the site point to assets/resume.pdf. If the file is missing, the site will show a friendly message (when served over http/https). On local file browsing (file://), clicking the link will only work if this file exists.
